You are a code flow analysis expert. Extract key entities from structured code descriptions.

## Extraction Requirements
Focus on code structure elements: [INPUTS], [OUTPUTS], [MAIN_FLOW], [COMMAND], [ALTERNATIVE_FLOW], [EXCEPTION_FLOW], etc.

### Entity Granularity:
- **Variables and data**: Extract individual variables, parameters, and data elements
- **Atomic operations**: Extract single operations like assignments, calculations, comparisons
- **Control structures**: Extract loops, conditions, function calls as separate entities
- **Keep labels concise**: Maximum 4-5 words per entity label

### Entity Types:
- input_data, output_data, variable
- assignment, calculation, comparison
- loop_start, loop_condition, loop_end
- condition, function_call, return

## Output Format (Strictly Follow):
EntityID | EntityLabel | EntityType

## Examples:
For "[INPUTS] <REF> trajectory_points </REF>: List[TrajectoryPoint]"
Output:
$N1 | trajectory_points | input_data

For "[COMMAND Initialize is_valid to True]"
Output:
$N2 | is_valid | variable
$N3 | set is_valid True | assignment

For "[COMMAND Compute segment_time_diff as abs(p1.time - p0.time)]"
Output:
$N4 | p0.time | variable
$N5 | p1.time | variable
$N6 | time difference | calculation
$N7 | absolute value | calculation
$N8 | segment_time_diff | variable

Extract entities from the following code description: